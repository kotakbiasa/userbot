name: Poetry Update

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  auto-update:
    runs-on: ubuntu-latest
    steps:
        - uses: fuzzylabs/gha-poetry-update@v1.0.2
          with:
            python-version: "3.13"
            poetry-version: "2.2.1"
